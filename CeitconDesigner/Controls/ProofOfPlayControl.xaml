<UserControl x:Class="Ceitcon_Designer.Controls.ProofOfPlayControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:local="clr-namespace:Ceitcon_Designer.Controls"
             xmlns:model="clr-namespace:Ceitcon_Data.Model.Network;assembly=Ceitcon_Data"
             xmlns:view="clr-namespace:Ceitcon_Designer.View"
             xmlns:converters="clr-namespace:Ceitcon_Designer.Converters"
             xmlns:behaviors="clr-namespace:Ceitcon_Designer.Behaviors"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style TargetType="{x:Type TextBlock}" BasedOn="{StaticResource StandartTextBlock}"/>
        <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource StandartTextBox}"/>
        <Style TargetType="{x:Type ScrollBar}" BasedOn="{StaticResource StandartScrollBar}"/>
        <Style x:Key="PlayerListBoxItem" TargetType="{x:Type ListBoxItem}">
            <Setter Property="AllowDrop" Value="True"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate>
                        <ContentPresenter Content="{TemplateBinding DataContext}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True" >
                    <Setter Property="FontWeight" Value="Bold" />
                    <Setter Property="Background" Value="Transparent" />
                </Trigger>
            </Style.Triggers>
            <Style.Resources>
                <DataTemplate DataType="{x:Type model:PlayerModel}">
                    <Border BorderThickness="1" BorderBrush="{StaticResource BackgroundDark}" Background="{StaticResource BackgroundLight}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="70" />
                                <ColumnDefinition Width="150" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding Name}" ToolTip="{Binding Identification}" Margin="5" Foreground="{StaticResource TextColor}" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                            <TextBlock Grid.Column="1" Text="{Binding HostName}" Margin="5" Foreground="{StaticResource TextColor}" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                            <TextBlock Grid.Column="2" Text="{Binding IPAddress}" Margin="5" Foreground="{StaticResource TextColor}" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                            <!--<TextBlock Grid.Column="3" Text="{Binding Port}" Margin="5" Foreground="{StaticResource TextColor}" VerticalAlignment="Center" HorizontalAlignment="Left"/>-->

                            <!--<Button Grid.Column="6" Style="{StaticResource ConfirmButton}"  Visibility="{Binding IsUnregistred, Mode=OneWay, Converter={x:Static converters:BoolToVisibilityConverter.Collapsed}}"-->
                            <Button Grid.Column="6" Style="{StaticResource ConfirmButton}" Visibility="{Binding IsRegistred, Mode=OneWay, Converter={x:Static converters:BoolToVisibilityConverter.InvertedCollapsed}}"
                                    ToolTip="Registred Player on Network" VerticalAlignment="Center" HorizontalAlignment="Right" Width="80" Margin="2,2,2,2"
                                    Command="view:CustomCommands.RegisterPlayer">
                                <TextBlock Text="Registred" FontSize="12" Margin="5" />
                            </Button>
                            <Button Grid.Column="6" Style="{StaticResource ConfirmButton}" Visibility="{Binding IsRegistred, Mode=OneWay, Converter={x:Static converters:BoolToVisibilityConverter.Collapsed}}"
                                    ToolTip="Edit Player licence information" VerticalAlignment="Center" HorizontalAlignment="Right" Width="60" Margin="2,2,85,2"
                                    Command="view:CustomCommands.EditPlayer">
                                <TextBlock Text="Edit" FontSize="12" Margin="5" />
                            </Button>
                            <Button Grid.Column="6" Style="{StaticResource ConfirmButton}" Visibility="{Binding IsRegistred, Mode=OneWay, Converter={x:Static converters:BoolToVisibilityConverter.Collapsed}}"
                                    ToolTip="Unregistred Player from Network" VerticalAlignment="Center" HorizontalAlignment="Right" Width="80" Margin="2,2,2,2"
                                    Command="view:CustomCommands.UnregisterPlayer">
                                <TextBlock Text="Unregistred" FontSize="12" Margin="5" />
                            </Button>
                        </Grid>
                    </Border>
                </DataTemplate>
            </Style.Resources>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="60" />
            <RowDefinition Height="30" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Background="{StaticResource HeaderBackground}">
            <TextBlock Text="FILTER LIST" Margin="5" Foreground="{StaticResource HeaderForeground}"/>
        </Grid>
        <StackPanel Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Stretch">
            <TextBlock Text="Player Name:" Margin="5" VerticalAlignment="Center" />
            <TextBox x:Name="tbName"  Margin="5" Width="150" MaxLength="50">
                <i:Interaction.Behaviors>
                    <behaviors:TextBoxEnterKeyUpdateBehavior />
                </i:Interaction.Behaviors>
            </TextBox>
            <TextBlock Text="Host Name:" Margin="5" VerticalAlignment="Center"/>
            <TextBox x:Name="tbHost" Margin="5" Width="100" MaxLength="50">
                <i:Interaction.Behaviors>
                    <behaviors:TextBoxEnterKeyUpdateBehavior />
                </i:Interaction.Behaviors>
            </TextBox>
            <TextBlock Text="IP Address:" Margin="5" VerticalAlignment="Center"/>
            <TextBox x:Name="tbIP" Margin="5" Width="100" MaxLength="15">
                <i:Interaction.Behaviors>
                    <behaviors:TextBoxEnterKeyUpdateBehavior />
                </i:Interaction.Behaviors>
            </TextBox>

            <TextBlock Text="Registred:" Margin="5" VerticalAlignment="Center"/>
            <CheckBox x:Name="cbRegistred" Margin="5,15,5,5" IsChecked="{x:Null}" IsThreeState="True"/>

            <Button Style="{StaticResource ConfirmButton}" ToolTip="Refresh list" VerticalAlignment="Bottom" HorizontalAlignment="Right" Width="130" Margin="15,5"
                    Click="RefreshButton_Click">
                <StackPanel Orientation="Horizontal" >
                    <Image Source="../Images/iconButton_Refresh.png" Width="22" Height="22" Margin="5"/>
                    <TextBlock Text="Refresh" FontSize="16" Margin="5,8,5,5"/>
                </StackPanel>
            </Button>
        </StackPanel>

        <Grid Grid.Row="2" Background="{StaticResource HeaderBackground}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="70" />
                <ColumnDefinition Width="150" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="PLAYER NAME" Margin="5" Foreground="{StaticResource HeaderForeground}"/>
            <TextBlock Grid.Column="1" Text="HOST NAME" Margin="5" Foreground="{StaticResource HeaderForeground}"/>
            <TextBlock Grid.Column="2" Text="IP ADDRESS" Margin="5" Foreground="{StaticResource HeaderForeground}"/>
            <TextBlock Grid.Column="3" Text="REGISTRED TIME" Margin="5" Foreground="{StaticResource HeaderForeground}"/>
            <TextBlock Grid.Column="4" Text="LAST CONNECTION" Margin="5" Foreground="{StaticResource HeaderForeground}"/>
            <TextBlock Grid.Column="5" Text="STATUS" Margin="5" Foreground="{StaticResource HeaderForeground}"/>
            <TextBlock Grid.Column="6" Text="" Margin="5" Foreground="{StaticResource HeaderForeground}"/>

        </Grid>

        <ListBox 
            Grid.Row="3"
            BorderThickness="0"
            HorizontalContentAlignment="Stretch"
            VerticalAlignment="Stretch"
            Background="Transparent"
            ScrollViewer.HorizontalScrollBarVisibility="Disabled"  
            ScrollViewer.VerticalScrollBarVisibility="Auto"
            ItemContainerStyle="{StaticResource PlayerListBoxItem}"
            SelectedItem="{Binding SelectedPlayer, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            ItemsSource="{Binding Players, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
    </Grid>
</UserControl>
