<UserControl x:Class="Ceitcon_Designer.Controls.AlertControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Ceitcon_Designer.Controls"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="200"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Image Stretch="Uniform" Source="../Images/iconAlert.png" HorizontalAlignment="Center"/>
        <TextBlock Grid.Row="1"
                   Foreground="Yellow"
                   FontSize="{Binding FontSize, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                   FontFamily="{Binding FontFamily, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"  
                   TextWrapping="Wrap"
                   HorizontalAlignment="{Binding HorizontalAlignment, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                   VerticalAlignment="{Binding VerticalAlignment, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                   Text="{Binding Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
    </Grid>
</UserControl>
