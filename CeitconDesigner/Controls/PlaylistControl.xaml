<UserControl x:Class="Ceitcon_Designer.Controls.PlaylistControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:view="clr-namespace:Ceitcon_Designer.View"  
             xmlns:playModel="clr-namespace:Ceitcon_Data.Model.Playlist;assembly=Ceitcon_Data"
             xmlns:converters="clr-namespace:Ceitcon_Designer.Converters"
             mc:Ignorable="d"
             x:Name="userControl">
    <Border BorderThickness="1" BorderBrush="{StaticResource BackgroundDark}" Background="{StaticResource BackgroundLight}">
        <Grid Margin="2">
            <TextBlock Margin="30,0,0,0" VerticalAlignment="Top" HorizontalAlignment="Left" Foreground="{StaticResource TextColor}">
                <TextBlock.Text>
                    <MultiBinding StringFormat="{}{0} ({1})">
                        <Binding Path="Name" />
                        <Binding Path="Duration" />
                       </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
            
            <Expander IsExpanded="True">
                <Grid Background="{StaticResource BackgroundLight}">
                    <Grid.Resources>
                        <Style TargetType="{x:Type TextBlock}" BasedOn="{StaticResource StandartTextBlock}">
                            <Setter Property="FontSize" Value="12" />
                        </Style>
                    </Grid.Resources>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Start Time:" Margin="5,10,0,0"/>
                    <TextBox Grid.Row="1" Text="{Binding StartTime, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, StringFormat={}{0:hh}:{0:mm}:{0:ss}, FallbackValue=00:00:00}" Width="80" MaxLength="9" Margin="5" HorizontalAlignment="Left"/>
                    <TextBlock Grid.Column="1" Text="Duration:" Margin="5,10,0,0"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Duration, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, StringFormat={}{0:hh}:{0:mm}:{0:ss}, FallbackValue=00:00:00}" Width="80" MaxLength="9" Margin="5" HorizontalAlignment="Left"/>
                    <TextBlock Grid.Column="2" Text="Auto" Margin="5,10,0,0"/>
                    <CheckBox Grid.Row="1" Grid.Column="2" Margin="5,10" IsChecked="{Binding Forever, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    <ContentPresenter Grid.Row="2" Grid.ColumnSpan="4" Content="{Binding AdditionalContent, ElementName=userControl}" />
                </Grid>
            </Expander>

            <Button Template="{StaticResource SimpleButton}" ToolTip="Remove Dependency" Command="view:CustomCommands.RemovePlaylist" VerticalAlignment="Top" HorizontalAlignment="Right">
                <Image Source="../Images/iconPlaylistItem_RemoveDependency.png" Width="20" Height="20" />
            </Button>
        </Grid>
    </Border>
</UserControl>
