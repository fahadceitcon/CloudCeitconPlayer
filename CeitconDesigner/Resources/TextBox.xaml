<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Ceitcon_Designer.Resources"
                     xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation">
    <DropShadowEffect x:Key="effectTextBox_Focused" BlurRadius="5" Color="#FF0086e3"></DropShadowEffect>

    <Style x:Key="StandartTextBox" TargetType="{x:Type TextBox}">
        <Setter Property="OverridesDefaultStyle" Value="True"></Setter>
        <Setter Property="Background" Value="#FF414141"></Setter>
        <Setter Property="BorderBrush" Value="#FFffffff"></Setter>
        <Setter Property="BorderThickness" Value="1"></Setter>
        <Setter Property="Padding" Value="0"></Setter>
        <Setter Property="FontSize" Value="14"></Setter>
        <Setter Property="Foreground" Value="#FFffffff"></Setter>
        <Setter Property="MinHeight" Value="26"></Setter>
        <Setter Property="MinWidth" Value="50"></Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBox}">
                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="2">
                        <ScrollViewer x:Name="PART_ContentHost" Style="{DynamicResource styleScrollViewer_TextBox}" VerticalAlignment="Center" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden" Margin="5,0" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter Property="BorderBrush" Value="#FF0086e3"></Setter>
                            <Setter Property="Effect" Value="{StaticResource effectTextBox_Focused}"></Setter>
                            <Setter Property="BorderThickness" Value="2"></Setter>
                            <Setter Property="Padding" Value="0"></Setter>
                            <Setter Property="Foreground" Value="#FF414547"></Setter>
                            <Setter Property="Background" Value="White"></Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ContextMenu" Value="{StaticResource TextBoxContextMenu}" />
    </Style>
    <ContextMenu x:Key="TextBoxContextMenu" Visibility="Collapsed" />
    <!--<ContextMenu x:Key="TextBoxContextMenu" Background="{StaticResource BackgroundLight}" Foreground="{StaticResource TextColor}">
        <MenuItem Command="ApplicationCommands.Copy" />
        <MenuItem Command="ApplicationCommands.Cut" />
        <MenuItem Command="ApplicationCommands.Paste" />
    </ContextMenu>-->
</ResourceDictionary>