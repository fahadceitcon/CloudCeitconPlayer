<Window x:Class="Ceitcon_Designer.View.ProofOfPlaySettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ceitcon_Designer.View"
        xmlns:model="clr-namespace:Ceitcon_Data.Model;assembly=Ceitcon_Data"
        xmlns:viewModel="clr-namespace:Ceitcon_Designer.ViewModel"
        xmlns:converters="clr-namespace:Ceitcon_Designer.Converters"
        xmlns:controls="clr-namespace:Ceitcon_Designer.Controls"
        xmlns:behaviors="clr-namespace:Ceitcon_Designer.Behaviors"
        mc:Ignorable="d"
        Title="Proof Of Play Settings" 
        WindowStartupLocation="CenterScreen" 
        WindowStyle="None"
        Height="160" Width="300"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        BorderThickness="2"
        BorderBrush="{StaticResource TextColor}"
        Background="{StaticResource BackgroundLight}">
    <Window.Resources>
        <Style TargetType="{x:Type TextBlock}" BasedOn="{StaticResource StandartTextBlock}"/>
        <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource StandartTextBox}"/>
        <!--<Style TargetType="{x:Type ScrollBar}" BasedOn="{StaticResource StandartScrollBar}"/>-->

    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="70"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Proof Of Play Settings" Style="{StaticResource StandartTextBlock}" HorizontalAlignment="Center" FontSize="18" />
        
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <TextBlock Text="Select Screen Number:" Style="{StaticResource StandartTextBlock}" Margin="10" VerticalAlignment="Center" />
            <TextBox Text="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type Window}}, Path=Screens, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                     Style="{StaticResource StandartTextBox}" 
                     ToolTip="Insert monitor number e.g. 1, 2, 3" Margin="0,10,0,10" VerticalAlignment="Center" HorizontalAlignment="Left" MaxLength="2" TextAlignment="Right"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="3" HorizontalAlignment="Center">
            <Button Content="Cancel" Style="{StaticResource ConfirmButton}" Margin="5" Height="40" Width="90" Click="CancelButton_Click"/>
            <Button Content="Apply" Style="{StaticResource ConfirmButton}" Margin="5" Height="40" Width="90" Click="OKButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
